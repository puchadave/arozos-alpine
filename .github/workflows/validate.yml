name: Validate webOwie_nodeOS

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  shell-syntax:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate scripts with Alpine /bin/sh
        run: |
          docker run --rm \
            -v "$PWD:/repo:ro" \
            alpine:3.24 \
            /bin/sh -c '
              set -eu
              sh -n /repo/installer/install-webowie-nodeos.sh
              sh -n /repo/installer/install-alpine-lxc.sh
              sh -n /repo/branding/apply-branding.sh
              sh -n /repo/scripts/update-webowie-nodeos.sh
            '
