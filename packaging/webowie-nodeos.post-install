#!/bin/sh
set -eu

chown -R webowie:webowie /var/lib/webowie-nodeos

rc-update add webowie-nodeos default >/dev/null 2>&1 || true
rc-service webowie-nodeos restart >/dev/null 2>&1 || rc-service webowie-nodeos start >/dev/null 2>&1 || true

exit 0
